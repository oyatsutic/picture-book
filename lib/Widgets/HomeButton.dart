import 'package:flutter/material.dart';

class HomeBurton extends StatelessWidget {
  const HomeBurton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset('assets/images/home_button'),
    );
  }
}
